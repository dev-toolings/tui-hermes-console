"use client";

import "@assistant-ui/react-markdown/styles/dot.css";
import { MarkdownText } from "@/components/run/markdown-text";

/** Même syntaxe et même hiérarchie Markdown sur toutes les surfaces de mission. */
export function XuluxMarkdownText() {
  return <MarkdownText />;
}
