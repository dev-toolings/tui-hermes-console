# G1-004D — rejeu réel Hermes sur VM 210

Date : 2026-08-04 23:24 Europe/Paris

## Cible et provider

- Proxmox vérifié avec `pvecli` : PVE `9.2.6`, nœud `pve` online.
- VM cible : `210`, `hermes-ephemeral-01`, état `running`, IP privée `192.168.1.210`.
- Accès guest : SSH strict avec la clé de rotation déjà autorisée et le fichier
  `known_hosts` dédié.
- Provider choisi : `openai-api`, modèle `gpt-5.4-nano`, base URL
  `https://api.openai.com/v1`.
- Le credential a été injecté depuis le credential pool Hermes local vers
  `/srv/hermes-console/workdir/auth.json` par stdin SSH, sans argument de
  processus, sans sortie de secret et sans fichier du dépôt.
- Contrôle distant : `auth.json` est `0600`, propriétaire `hermes-console`;
  `hermes auth list openai-api` expose une seule entrée labellisée
  `console-g1-004-openai`.
- `/api/model/options` distant : `openai-api authenticated=true current=true`,
  `gpt-5.4-nano`, 16 modèles.

## Rejeu P-E2E

Le Console réel a été lancé avec PostgreSQL éphémère et relié à Hermes VM210
par un tunnel SSH local vers `127.0.0.1:18643` → guest `127.0.0.1:8642`.
Le token runtime a été lu uniquement en mémoire depuis `runtime.env`.

Résultat de `bun run proof:g1-004-real` :

```text
positive=real-console-route policy=pass no-terminal-effect=pass
negative=real-console-route policy-unavailable=pass remote-untouched=pass
recovery=pass
```

La phase recovery a aussi validé la reconnexion après redémarrage Console : la
réconciliation a repris le run (`resumed=1`), puis la réponse approval a
rebranché le flux local sans arrêter le run distant.

## Correctif runtime couvert

Après reproduction réelle de la course, un flux actif issu de la reconciliation
est maintenant identifié séparément du flux initial. La réponse approval force
une reconnexion uniquement pour ce flux repris; l’ancien lecteur est annulé
avec la raison interne `reconnect`, sans transformer cela en annulation Hermes.

Validation ciblée : `18 pass`, `0 fail`; `bun run typecheck` vert.

## Limites restantes

- La revue indépendante et le rejeu opérateur 2 doivent encore être signés par
  leurs personnes respectives.
- Aucun secret n’est consigné ici.
- Aucun commit, push ou déploiement n’a été effectué.
