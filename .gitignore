node_modules/
.env
.env.local
# Identifiants de test en clair — ne doit jamais partir dans un commit
auth.md
*.png
*.jpg
*.jpeg
*.gif
*.webp
# Icônes de l'app desktop : `tauri.conf.json` les référence pour le bundle,
# la règle `*.png` ci-dessus les excluait et `tauri build` cassait au clone.
!apps/web/src-tauri/icons/**/*.png
# Visuel d'onboarding généré, référencé par le bundle Vite.
!apps/web/src/assets/setup-operation-board.png
# Décision du 08-08-2026 : les captures de preuve ne sont plus versionnées. Les rapports
# d'evidence portent une description observable et l'empreinte `sha256` de la capture,
# qui reste hors dépôt. Ne pas rouvrir d'exception ici pour docs/user-stories/evidence/.
.playwright-mcp/
.impeccable/
.playwright-cli/
